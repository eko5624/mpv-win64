pkgname=highway-dev
pkgver=0.16.0
pkgrel=1
pkgdesc='C++ library that provides portable SIMD/vector intrinsics'
arch=('x86_64')
url="https://github.com/google/highway/"
license=('CUSTOM')
source=("git+https://github.com/google/highway")
md5sums=('SKIP')

build() {
  cd $srcdir/highway
  rm -rf build && mkdir build && cd build
  $CMAKE .. -G "MinGW Makefiles" -DCMAKE_INSTALL_PREFIX=/opt -DCMAKE_BUILD_TYPE=Release \
    -DBUILD_GMOCK=OFF \
    -DBUILD_TESTING=OFF \
    -DCMAKE_GNUtoMS=OFF \
    -DHWY_CMAKE_ARM7=OFF \
    -DHWY_ENABLE_CONTRIB=OFF \
    -DHWY_ENABLE_EXAMPLES=OFF \
    -DHWY_ENABLE_INSTALL=ON \
    -DHWY_SYSTEM_GTEST=OFF \
    -DHWY_WARNINGS_ARE_ERRORS=OFF \
    -DINSTALL_GTEST=OFF
  $CMAKE --build .
}  
  
package() {
  cd $srcdir/highway/build
  $CMAKE --install . --prefix=$pkgdir/opt
}  
